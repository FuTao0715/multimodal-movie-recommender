# 🧠 四种推荐模型对比分析

> 数据集：MovieLens 100K（610 用户，9742 电影，100,836 评分）
> 评估方式：全部 610 用户 80/20 分层 holdout

---

## 1. 模型总览

| 模型 | 文件 | 框架 | 算法类型 | 参数 | RMSE |
|------|------|------|----------|------|------|
| 🧬 CF+Genre | `keras_cf_ncf.pt` | Keras→PyTorch | Neural CF + 类型 | 74 万 | **0.716** |
| 🌲 RandomForest | `randomforest_recommender.pkl` | scikit-learn | 随机森林回归 | 100 棵树 | 0.784 |
| 🎬 多模态 NCF | `multimodal_ncf_best.pt` | PyTorch | 神经协同过滤 | 149 万 | 0.788 |
| 📝 纯文本 NCF | `textonly_ncf.pt` | PyTorch | 神经协同过滤 | 90 万 | 0.792 |

---

## 2. 各模型详解

### 🧬 CF + 类型特征 NCF（RMSE 0.7163 🥇）

**算法：** 神经协同过滤（Neural Collaborative Filtering）

**输入特征（148 维）：**
```
User Embedding (64 维) ──┐
Movie Embedding (64 维) ─┼── Concat(148) ──► MLP(256→128→64→1) ──► 预测评分
20 维电影类型 (multi-hot) ┘
```

**核心技术：**
- Embedding 层学习用户/电影的隐语义向量
- 20 维多热编码：直接从 MovieLens 的 `movies.csv` 提取（Action, Comedy, Drama 等）
- MLP 三层全连接 + BatchNorm + Dropout(0.3) + L2 正则化
- **线性输出**（无范围约束），可能预测超出 [0.5, 5.0]
- Adam 优化器，学习率 0.000125

**✅ 优点：**
- **RMSE 最低**（0.7163），领先第二名 ~8.7%
- 参数量最小（74 万），推理最快
- 电影类型是强特征 —— 同类型电影评分模式相似
- 覆盖全部 9724 部电影（不依赖 TMDB 富化数据）

**❌ 缺点：**
- 线性输出无范围约束，偶尔预测 >5.0 或 <0.5（已在推荐界面裁剪）
- 无内容特征（没有海报图像、剧情文本）
- 无训练历史，仪表板看不到 Loss/RMSE 曲线
- 原始为 Keras 3 格式，需转换才能在 PyTorch 环境中使用

---

### 🌲 随机森林（RMSE 0.7843 🥈）

**算法：** RandomForestRegressor（随机森林回归）

**输入特征（4 维）：**
```
user_avg_rating     — 用户历史平均评分
movie_avg_rating    — 电影历史平均评分
log1p(user_count)   — 用户评分数（对数平滑）
log1p(movie_count)  — 电影被评数（对数平滑）
```

**核心技术：**
- 100 棵决策树，最大深度 12
- 特征标准化（StandardScaler）
- 用户/电影均值计算时排除目标评分（避免数据泄露）

**✅ 优点：**
- **简单且有效** —— 仅 4 个统计特征就超越两个深度学习模型
- 可解释性强：可输出特征重要性（哪个特征对预测贡献最大）
- 训练快（秒级），不需要 GPU
- 鲁棒性好：对超参数不敏感，不易过拟合
- 能预测冷启动用户（用全局均值填充）

**❌ 缺点：**
- 特征工程完全手工，表达能力有限
- 只能预测已见过的用户/电影组合范围
- 未利用任何内容信息（图像、文本、类型）
- 推理需逐条构造特征，批量评估慢（~23s vs NCF 的 ~3s）

---

### 🎬 多模态 NCF（RMSE 0.7881 🥉）

**算法：** 多模态神经协同过滤

**输入特征（640 维）：**
```
User Embedding (64) ────────┐
Movie Embedding (64) ───────┤
ResNet50 图像特征 (2048→256) ├── Concat(640) ──► MLP(256→128→64→1) ──► Sigmoid×4+1 → [1,5]
MiniLM 文本特征 (384→256) ──┘
```

**核心技术：**
- **图像分支：** ResNet50（在 ImageNet 预训练）提取海报特征 → 2048 维 → Linear 投影到 256 维
- **文本分支：** MiniLM（sentence-transformers）编码剧情简介 → 384 维 → Linear 投影到 256 维
- **协同过滤：** 用户/电影各 64 维 Embedding
- **融合：** 拼接后经 3 层 MLP（256→128→64→1）+ BatchNorm + Dropout
- **输出缩放：** `sigmoid(x) * 4 + 1` 将输出约束在 [1, 5]

**✅ 优点：**
- 三种信息源融合：协同信号 + 视觉内容 + 语义内容
- 输出自然约束在合理范围（Sigmoid 缩放）
- 完整的训练历史（Loss/RMSE/MAE 曲线可展示）
- 有 BatchNorm + Dropout 防止过拟合

**❌ 缺点：**
- **图像特征贡献极小** —— 去掉图像仅损失 ~0.004 RMSE
- 参数量最大（149 万），推理需加载图像/文本特征
- 依赖预提取特征（ResNet50 + MiniLM，~130MB 特征文件）
- 只能在有 TMDB 数据的 ~9734 部电影上推荐
- 训练耗时（每 epoch 需前向 ResNet50 和 MiniLM）

---

### 📝 纯文本 NCF（RMSE 0.7916）

**算法：** 神经协同过滤（消融实验）

**输入特征（384 维）：**
```
User Embedding (64) ────────┐
Movie Embedding (64) ───────┼── Concat(384) ──► MLP(256→128→64→1) ──► Sigmoid×4+1 → [1,5]
MiniLM 文本特征 (384→256) ──┘
```

**核心技术：** 与多模态 NCF 相同，但去掉图像分支，用于量化海报贡献。

**✅ 优点：**
- 比多模态轻量（90 万 vs 149 万参数）
- 性能几乎等同多模态（RMSE 仅差 0.004）
- 有完整的训练历史（11 轮）
- 输出约束在 [1, 5]

**❌ 缺点：**
- 文本特征贡献有限（没有用电影类型）
- 与多模态相同依赖 TMDB 特征

---

## 3. 横向对比

### 3.1 性能指标

|  | CF+Genre | RandomForest | 多模态 NCF | 纯文本 NCF |
|------|----------|-------------|-----------|-----------|
| RMSE | 🥇 0.716 | 🥈 0.784 | 🥉 0.788 | 0.792 |
| Acc@0.5 | 🥇 55.1% | 51.2% | 51.5% | 51.1% |
| Acc@1.0 | 🥇 85.6% | 82.0% | 82.1% | 81.3% |
| 相对基线提升 | 15.4% | 7.4% | 6.9% | 6.5% |

（基线 = 用户+电影均值预测 RMSE 0.847）

### 3.2 资源消耗

| | CF+Genre | RandomForest | 多模态 NCF | 纯文本 NCF |
|------|----------|-------------|-----------|-----------|
| 参数量 | 74 万 | 100 棵树 | 149 万 | 90 万 |
| 模型大小 | 8.5 MB | 34 MB | 18 MB | 3.6 MB |
| 推理速度 | ⚡ 快 | 🐢 慢 | ⚡ 快 | ⚡ 快 |
| 需要 GPU | 否 | 否 | 建议 | 建议 |
| 额外依赖 | Keras(原版) | sklearn | torch, torchvision | torch, sentence-transformers(提取) |

### 3.3 特征覆盖

| 特征类型 | CF+Genre | RandomForest | 多模态 NCF | 纯文本 NCF |
|----------|:--------:|:------------:|:----------:|:----------:|
| 协同信号 (Embedding) | ✅ | ❌ | ✅ | ✅ |
| 统计特征 | ❌ | ✅ | ❌ | ❌ |
| 电影类型 (20维) | ✅ | ❌ | ❌ | ❌ |
| 海报图像 (2048维) | ❌ | ❌ | ✅ | ❌ |
| 剧情文本 (384维) | ❌ | ❌ | ✅ | ✅ |
| 特征总数 | 148 | 4 | 640 | 384 |

---

## 4. 关键洞察

### 为什么 CF+Genre 最强？

电影类型是**直接的内容标签**（这部电影是动作片还是文艺片），比从海报/文本中**间接推断**更可靠。20 维多热编码虽然维度低，但信息密度极高——它直接告诉模型"这是动作+科幻"而不是让模型从 2048 维像素中猜。

### 为什么图像特征没用？

- MovieLens 的海报风格多样（不同年代、不同国家），ResNet50 在 ImageNet 上学的特征（物体、场景）与"这部电影打几分"关联很弱
- 文本特征略有用——剧情简介的语义能反映电影类型和质量

### 为什么简单模型也能打？

随机森林仅用 4 个统计特征就超过两个深度学习模型，说明：
- **用户平均分** 是最强基线（用户评分习惯固定）
- **电影平均分** 是第二强基线（好电影大家都喜欢）
- MovieLens 数据集的评分信号本身就不难预测（信息天花板 ~0.65-0.70）

---

## 5. 结论

| 如果你想... | 推荐模型 |
|-------------|----------|
| 🏆 **追求最佳准确率** | CF+Genre |
| 📖 **写论文展示多模态融合** | 多模态 NCF |
| 🔬 **做消融实验** | 纯文本 NCF vs 多模态 NCF |
| 🏭 **快速部署上线** | 随机森林 |
| 🎓 **学习推荐系统** | 四个都跑一遍 |

---

*分析日期：2026-06-23*
